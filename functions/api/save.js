export async function onRequestGet({env}){const row=await env.DB.prepare("SELECT state,updated_at FROM saves WHERE id='default'").first();return Response.json({save:row?JSON.parse(row.state):null,updated_at:row?.updated_at||null})}export async function onRequestPost({request,env}){try{const body=await request.json();if(!body.player)return new Response('bad request',{status:400});await env.DB.prepare("INSERT INTO saves(id,state,updated_at) VALUES('default',?,?) ON CONFLICT(id) DO UPDATE SET state=excluded.state,updated_at=excluded.updated_at").bind(JSON.stringify({player:body.player,elapsed:body.elapsed}),Date.now()).run();return Response.json({ok:true})}catch{return new Response('bad request',{status:400})}}